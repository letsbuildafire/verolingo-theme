title: PLUGIN_ADMIN.INDEX
extends@: modular

form:
  fields:
    tabs:

      fields:
        advanced:

          fields:
            columns:

              fields:
                column1:

                  fields:
                    header.body_classes:
                      replace-default@: page--index
